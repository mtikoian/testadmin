/*** ================================================================================ Name : . . Author : Description : Revision: $Rev$ URL: $URL$ Last Checked in: $Author$ =============================================================================== ***/ IF EXISTS ( SELECT 1 FROM sys.indexes si JOIN sys.objects so ON si.object_id = so.object_id JOIN sys.schemas ss ON so.schema_id = ss.schema_id WHERE si.name = '' AND so.name = ' ' AND ss.name = ' ') BEGIN DROP INDEX ON  . ; END CREATE INDEX ON  . ( -- Add columns here, in order ) INCLUDE ( -- Add list of included columns here if required. -- Otherwise remove this section ) WITH ( FILLFACTOR=90, SORT_IN_TEMPDB=OFF )  